name: status
on: 
    schedule: 
      - cron: '25 * * * * '

jobs:
    check_status:
      runs-on: ubuntu-latest
      steps:
        - name: Check Published Status
          run: | 
            '[ "success" = "$(gh run list -b main -w 'publish' | awk 'NR==1{print $2}')" ]'

